# 第十届工业信息安全技能大赛工控安全锦标赛

## Crypto

### GY10-Audit

> 为了节省空间，某内部系统号称采用“自研高强度密码模块”把敏感日志 AES 加密后上传，又用 RSA 对关键密钥做了“备份加密”，最后还用“秘密分享”来保护真正的 FLAG。
> 不幸的是，开发同学对“随机数”和“密码学”理解似乎有点……偏差。
> 你从审计服务器上拿到了以下文件
> 你的任务是：从这些文件中还原出最终的 flag。

### GY10-GhostPlatoon

> 你截获了一段编队行驶期间的认证流量，以及一份封装着机密的密文。Flag 格式为flag{}

### GY10-PLC 网关异常响应排查

> 水处理厂的 PLC 网关近期出现了异常控制行为，运维人员从现场抓取到了一段上位机与 PLC 之间的通信流量（PCAP），同时还拿到了相关的协议源码与客户端示例。表面上看，这些报文只是正常的遥测上报与状态查询，但进一步排查后发现，攻击者似乎正在利用某种工控协议的加密机制缺陷，伪造一条能够让 PLC 进入维护模式的控制报文。请你作为应急响应人员，结合流量包与源码，分析协议结构，定位可利用的通信样本，恢复报文中的关键材料，构造目标控制指令，并最终从本地模拟器中获取正确的 Flag。
> Flag 格式为：flag{}

### GY10-TwinModuli

> 也许差的不只是一个 δ。  
> 也许诊断通道里的噪声，才是你真正该听见的东西。Flag 格式为flag{}

### GY10-工控网关密钥泄露

> 某工业控制网关在一次安全巡检中被发现存在异常加密通信行为，运维人员从设备中导出了疑似固件文件和一份加密数据文件。初步分析表明，该固件内部可能硬编码了用于保护敏感数据的加密密钥，而加密文件中则保存着本次演练的最终秘密。请你作为安全分析人员，对固件文件进行深入分析，提取其中隐藏的关键配置，破解加密文件，并还原最终的 Flag。Flag 格式为：flag{}

## Reverse

### GY10-AutoCrypt-ADB

> 描述：
> 我们通过车机的adb pull拉下来了车机的adb验证程序，请你分析一下。服务器地址192.168.18.20。Flag 格式为flag{}
> URL：
> 192.168.18.20:1337

### GY10-Driver

> 有一天小明给了你一个样本，说可以通过执行一个应用层程序自动获得flag！但是他不小心把应用层程序弄丢了。
> 你作为一个经验丰富的逆向专家，需要分析该驱动程序，并编写应用层程序通过与驱动进行通信获得flag。

### GY10-VCL-FW

> 我们从一台车机的 OTA 升级包里提取到加密的 MCU 固件 `firmware.enc`，
> 以及升级程序里的固件校验器 `fw_check`,请你逆向 `fw_check`，弄清真实加解密路径，离线解开 `firmware.enc`。Flag 格式为flag{}

### GY10-反应釜温度与压力融合控制

> 某工业控制系统在 S7-1200 PLC 中实现了一套参数校准与输出计算逻辑，系统要求输入初始标定值 R1 = INT#2026，并结合内部固定参数进行多级加法、乘法与减法运算后得到输出 OUT，最终定义系统安全标识 FLAG = 2026 + OUT，请根据梯形图逻辑计算最终 FLAG 值。Flag 格式：flag{...}
> SIMATIC STEP 7 incl. Safety and WinCC V16 TRIAL Download：
> https://support.industry.siemens.com/cs/document/109772803/simatic-step-7-incl-safety-and-wincc-v16-trial-download?dti=0&lc=en-CN
>
> 西门子TIA 博途V16安装详解及授权：
> https://blog.csdn.net/shmp54/article/details/136840560
>
> 西门子博途V16安装全网最详解教程：
> https://zhuanlan.zhihu.com/p/304315391
>
> 请下载以上软件，进行环境配置。

## Pentest

### GY10-零信任架构下进行的攻防对抗环境

> 说明： 
> 1. 业务网络信息：
> 集成管控平台(控制平面)：172.16.233.11(管理IP+业务IP)
> 路由转发系统(数据平面/零信任加密流量交换中心)：172.16.233.12(管理IP+业务IP)
> 接入网关系统-服务端(数据平面/业务服务端)：172.16.233.13(管理IP+业务IP)
> 接入网关系统-靶标01(基于某CMS系统漏洞)：192.168.222.2(Docker容器IP，位于172.16.233.13同一台机器)
> 接入网关系统-靶标02(基于某前端RSC的漏洞)：192.168.222.3(Docker容器IP，位于172.16.233.13)
> 2. 靶标Flag文件信息：
> 靶标01需要选手通过渗透攻击的方式成功登录，或者绕过认证访问的方式登录，并在左侧 访问控制-身份管理 处获取Flag内容。示例如下：
> (1) 访问 访问控制-身份管理 页面，页面上会有一条 PCLCR_FLAG03 的记录
> (2) 点击该条目后面的 编辑 按钮，在弹出窗口下方 备注 中保存有Flag内容
> 靶标02、靶标03位于容器内的 [ /flag.txt ] 内，选手需要通过利用PoC工具或其他方式读取该文件，文件内写有Flag内容。示例如下：
> (1) 执行命令: ./poc.py --url http://127.0.0.1:3000 --cmd "cat /flag.txt"
> (2) 获得Flag结果: You Captured the Flag! The Flag is: [ this_is_the_flag_please_copy_it ]
>
>
> 根据以上信息，完成指定任务，并获取Flag提交。Flag是32位长度，数字及小写字母混合
> 第一道题：零信任产品平台的攻击（300分）：攻破部署在数据中心区的应用A的访问权限，获取关键信息，并获应用A服务器上的文件内容。
> 第二道题：在成功击穿零信任防线后，选手获得内部网络入口，可对后方的某CMS系统发起攻击（300分）：对后方的某CMS系统发起渗透并获取服务器上的指定敏感文件（Flag）。
> 第三道题：攻破零信任平台后，选手基于RSC的漏洞服务发起攻击（300分）：基于RSC的漏洞的远程代码执行漏洞，获取服务器上的指定敏感文件（Flag）。

### GY10-智能汽车安全

> 某智能汽车公司研发的一套智联域控制器（ICC）镜像靶标，Ip如下：172.16.201.0/24，请尝试从系统漏洞挖掘、虚拟车载通信 OS 深度渗透、虚拟整车总线报文分析、虚拟本地接口安全测试等角度进行漏洞挖掘，并将成果提交到系统。

## Pwn

### GY10-furnace

> 某车联网云端“熔炉”模块负责对车载ECU固件做投料、淬炼与化验。在严密的安检与工艺门控下完成锻造，即可拿到通往车端机密（flag）的钥匙。服务器地址192.168.18.21。Flag 格式为flag{}
> URL：
> 192.168.18.21:7777

## Misc

### GY10-look

> look。

### GY10-泵站异常写寄存器排查

> 某泵站 PLC 在夜间出现短暂异常，现场 HMI 日志显示 2 号泵的运行参数被修改。运维人员导出了当时的网络流量、HMI 操作日志和寄存器说明表。请你分析 Modbus/TCP 流量，定位异常写寄存器操作，并还原被写入寄存器中的隐藏信息。Flag 格式为：flag{}

### GY10-工控协议流量迷局

> 某 ICS 系统截获了一批混合协议流量，其中存在异常数据通信。细心分析后发现，流量中藏有关键线索——请仔细分析这些流量。Flag 格式：flag{...}

### GY10-时间域隐蔽通道-“扫描幽灵”

> 某 SCADA 系统定期轮询 PLC，流量包间隔精确到毫秒——一切看起来都很正常。但仔细统计时间间隔后发现：某次轮询周期内出现了异常密集的请求窗口，响应速度远超正常值。这些额外响应包里藏着什么？Flag 格式：flag{...}

### GY10-水厂夜班异常

> 某水处理站夜班期间出现工艺参数异常，值班人员确认当时没有执行相关控制操作。现场保存了网络流量、HMI 配置备份、PLC 逻辑导出、设备资产清单和报警记录。请结合附件还原异常控制行为，并确认导致工艺异常的关键控制点及对应现场设备。Flag 格式为flag{}。

### GY10-幽灵PLC

> 某水处理厂的核心工控网络近期出现异常波动，安全专家怀疑控制网段已遭渗透。运维人员紧急抓取了一段上位机与核心 PLC 之间的双向通信流量（PCAP）。初步排查显示，大量通信看似是正常的传感器数值读取，但情报指出，黑客正利用某种合法的工业协议功能，悄悄向 PLC 中切片注入一段恶意固件压缩包；同时，为了规避检测，黑客的解密密钥似乎通过其他隐蔽信道进行了“带外传输”。请你作为应急响应专家，深度剖析这份流量包，剥离背景噪音，提取出被注入的固件本体，破解密钥，并找出隐藏在固件底层的最终秘密。Flag格式为：flag{}
